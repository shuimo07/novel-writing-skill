# 本地配置（占位值，复制为 .env 后填写）

服务端只读这个文件；`.env` 已被 .gitignore 忽略，**不要把真实 Key 提交进仓库**。

```
# ── DeepSeek ─────────────────────────────────────────────
# 只放服务端环境变量。不要加 VITE_ 前缀（那会把 Key 打进前端包），
# 不要写进浏览器持久存储、仓库、日志、Skill、备份或错误详情。
DEEPSEEK_API_KEY=

# 模型名：默认 deepseek-flash，保留服务端配置入口
DEEPSEEK_MODEL=deepseek-flash

# ── 本地服务 ─────────────────────────────────────────────
# 只绑定 127.0.0.1，不接受外部访问；生产构建由它同源托管 dist/web
PORT=8787

# ── 生成参数 ─────────────────────────────────────────────
# 分析与归纳固定 temperature=0.2、max_tokens=4096（见 src/shared/limits.ts）
# 试写可调；基础版与加 Skill 版始终使用同一组参数
TRYOUT_TEMPERATURE=1.0

# ── 开发用 Mock ──────────────────────────────────────────
# 只有显式设为 1 才启用。Mock 结果带 Mock 标记，且不能导出为正式作者 Skill。
# 缺 Key 时不会自动退化成 Mock，而是显示“未配置真实分析”。
ALLOW_MOCK_ANALYSIS=0
```

## 本地启动

```bash
cd apps/writing-style-lab
npm install
cp .env.example .env        # 填入 DEEPSEEK_API_KEY（没有 Key 也能写作、备份、查看结果）
npm run dev                 # 前端 http://127.0.0.1:5173 ，API 走 Vite 代理
```

生产构建（本地同源托管）：

```bash
npm run build               # 类型检查 + 前端构建到 dist/web
npm start                   # http://127.0.0.1:8787
```

## 没有 API Key 时

写作、导入、直接采样、自动保存、备份恢复、规则确认、Skill 预览与导出（不含模型部分）全部可用；
分析与试写会明确显示“未配置真实分析”，不会伪造任何结果。
